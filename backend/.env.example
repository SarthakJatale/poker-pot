# Server Configuration
PORT=3001

# CORS Configuration - URL of your frontend application
CLIENT_URL=http://localhost:5173

# Environment (development, production, test)
NODE_ENV=development

# Optional: Database URL (for future use)
# DATABASE_URL=mongodb://localhost:27017/poker-pot

# Optional: JWT Secret (for future authentication)
# JWT_SECRET=your-super-secret-jwt-key

# Optional: Redis URL (for future session management)
# REDIS_URL=redis://localhost:6379
